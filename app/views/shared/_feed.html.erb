<% if @feed_items.any? %>
  <ol class="microposts">
  	<!-- here, rails know to call the micropost partial because each
  		element of @feed_item has class Micropost -->
  	<%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>