<!-- added from listing 11.35; we put in embedded ruby for an if else
	branch -->
	<!-- i made the partials -->
<% provide(:title, "Home") %>

<% if logged_in? %>
	<%= render 'static_pages/user' %>
<% else %>
	<%= render 'static_pages/home' %>
<% end %>
